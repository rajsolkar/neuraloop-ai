import { WorkflowsView } from "@/components/workspace/workflow-list-view";

export default function WorkflowsPage() {
  return (
    <WorkflowsView
      heading="My Workflows"
      description="All your workflows, in one place."
    />
  );
}